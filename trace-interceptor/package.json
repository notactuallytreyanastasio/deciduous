{
  "name": "deciduous-trace-interceptor",
  "version": "0.1.0",
  "description": "Fetch interceptor for capturing Claude API traffic to deciduous",
  "main": "dist/interceptor-loader.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "bundle": "esbuild src/interceptor-loader.ts --bundle --platform=node --target=node18 --outfile=dist/bundle.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}
