defmodule Deciduex do
  @moduledoc """
  Elixir replacement for deciduous CLI commands.

  Reads the SQLite database at `.deciduous/deciduous.db` and provides
  equivalent functionality to the Rust `deciduous` binary.
  """
end
